# GPT-GNN — checkpoint for DeepFP_Prep

## Expected file

```text
GPT_GNN/model.pth
```

On the GraphMVP shared Drive the file is **`GPT_TNN.pth`**. **Rename** to **`model.pth`** in this folder (folder name stays **`GPT_GNN`** to match `env_utils.py`).

## Download

1. [GraphMVP Google Drive folder](https://drive.google.com/drive/folders/1uPsBiQF3bfeCAXSDd4JfyXiTh-qxYfu6?usp=sharing)
2. Download **`GPT_TNN.pth`** → save as **`GPT_GNN/model.pth`**.

## Reference

- Path: `GPTGNN_PATH` in `../../env_utils.py`
- Index: `../WEIGHTS_DOWNLOAD_LIST.txt`
